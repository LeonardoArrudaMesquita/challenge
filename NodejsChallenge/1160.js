var input = require('fs').readFileSync('1153.txt', 'utf8');
var lines = input.split('\r\n');

dado = lines.split(" ");

for (let i = 0; i + dado[0] > dado[1] * dado[3]; i++) {    
    
    if (dado[0]) {
        
    }
}